<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd"> <!--mapper.dtd-->
<mapper namespace="mapper"> <!-- mapper 로 전체를 감싸고 있다. -->
    <select id="selectDate" resultType="java.util.Date">
        SELECT NOW()
    </select>
</mapper>